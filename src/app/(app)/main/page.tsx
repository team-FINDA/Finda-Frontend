'use client';

import styled from '@emotion/styled';
const page = () => {
  return <Container>페이지</Container>;
};

export default page;

export const Container = styled.div`
  width: 100%;
  height: 100%;
`;
